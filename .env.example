# Cấu hình ứng dụng
PORT=3000
NODE_ENV=development

# Secret key cho session (thay đổi khi deploy)
SESSION_SECRET=ai-product-store-secret-key-change-me-in-production

# Cookie settings (set COOKIE_SECURE=false nếu chưa có HTTPS)
COOKIE_SECURE=false

# Database (SQLite - không cần cấu hình thêm)
DB_PATH=./data/database.sqlite
